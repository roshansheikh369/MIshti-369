mishtee_css = """
/* Main Container & Background */
.gradio-container {
    background-color: #FAF9F6 !important;
    font-family: 'Helvetica Neue', Arial, sans-serif;
    color: #333333;
}

/* Typography - Headings (Serif, Spaced) */
.prose h1, .prose h2, .prose h3, .prose h4 {
    font-family: 'Georgia', 'Times New Roman', serif !important;
    letter-spacing: 0.05em;
    font-weight: 400;
    color: #333333;
}

/* Typography - General Text & Tables (Light Sans-Serif) */
.prose, .markdown, p, span, label {
    font-weight: 300 !important;
}

/* Tables specifically */
.dataframe, table {
    font-family: 'Helvetica Neue', Arial, sans-serif !important;
    font-weight: 300;
    border: 1px solid #E5E5E5 !important;
}

/* Buttons (Sober Terracotta, Sharp Lines) */
button {
    background-color: #C06C5C !important;
    color: #FFFFFF !important;
    border: none !important;
    border-radius: 0px !important; /* Strict constraint: No rounded shapes */
    box-shadow: none !important;   /* Strict constraint: No shadows */
    padding: 12px 24px !important;
    transition: opacity 0.2s ease;
}

button:hover {
    opacity: 0.9;
}

/* Input Fields & Text Areas (Sharp, Thin Borders) */
input, textarea, select, .gr-input {
    background-color: #FFFFFF !important;
    border: 1px solid #CCCCCC !important;
    border-radius: 0px !important;
    box-shadow: none !important;
}

/* Panel/Group Spacing */
.gradio-group, .gradio-row, .gradio-column {
    gap: 2rem !important; /* Significant whitespace */
    background: transparent !important;
    border: none !important;
}

/* Remove default Gradio rounded corners on all block elements */
* {
    border-radius: 0px !important;
}
"""
